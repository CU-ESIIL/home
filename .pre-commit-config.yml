repos:
  - repo: local
    hooks:
      - id: front-matter
        name: Ensure curated pages have tags and date
        entry: python scripts/check_front_matter.py
        language: system
        files: '^docs/(quickstart|container-library)/.*\.md$'
